
local customHWID = "xxxxxxxx-xxxx-xxxx-xxxxxxxxxxxx"
local analyticsService = game:GetService("RbxAnalyticsService")

local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
    if self == analyticsService then
        local method = getnamecallmethod()
        if method == "GetClientId" or method == "get_client_id" then
            return customHWID
        end
    end
    return oldNamecall(self, ...)
end))

if analyticsService.GetClientId then
    local oldGetClientId
    oldGetClientId = hookfunction(analyticsService.GetClientId, newcclosure(function(self)
        return customHWID
    end))
end

local oldIndex
oldIndex = hookmetamethod(game, "__index", newcclosure(function(self, key)
    if self == analyticsService and (key == "GetClientId" or key == "get_client_id") then
        return function() return customHWID end
    end
    return oldIndex(self, key)
end))

local player = game:GetService("Players").LocalPlayer
local androidID = (gethwid and gethwid()) or "unknown"
local function buildKey(id) return (id):reverse() end
local VALID_KEY = ("c6dde8ef621767784ffaf227b71bb40414a9c7126a2f27f367f2b3227fe72260"):reverse()
local currentKey = buildKey(androidID)

if currentKey ~= VALID_KEY then
    task.defer(function() player:Kick("??!!??") end)
    return
end

local safe_loadstring = clonefunction and clonefunction(loadstring) or loadstring
local safe_pcall = clonefunction and clonefunction(pcall) or pcall
local safe_HttpGet = clonefunction and clonefunction(game.HttpGet) or game.HttpGet
local safe_hookfunc = clonefunction and clonefunction(hookfunction) or hookfunction

local bananaKeyBytes = {169, 124, 245, 184, 163, 165, 208, 43, 198, 172, 116, 191}
local maruKeyBytes = {0}

local function getRealKey(bytes)
    local k = ""
    for _, byteVal in ipairs(bytes) do k = k .. string.char(byteVal) end
    return k
end

local ANYAxJPA = getgenv().ANYAxJPA
local targetURL = ""
local targetKey = ""
local extraSetup = function() end 

if ANYAxJPA == "BananaCat" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/main/BananaHub.lua"
    
elseif ANYAxJPA == "BananaCatKaitunLevi" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/BananaCat-KaitunLevi.lua"
    
elseif ANYAxJPA == "BananaCatBountyM1" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/Bountynew.lua"
    
elseif ANYAxJPA == "BananaCatBountyOLD" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/BananaCat-BountyEz.lua"
    
elseif ANYAxJPA == "BananaCatOneClickV4" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/NewV4Config.lua"
    
elseif ANYAxJPA == "BananaCatMainV4" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/BananaCat-KaitunV4"
    
elseif ANYAxJPA == "BananaCatKaitunLevel" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/main/BananaCat-kaitunBF.lua"
    
elseif ANYAxJPA == "BananaCatPVP" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/BloxFruitPVP.lua"
    
elseif ANYAxJPA == "BananaCatFindFruit" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/HopFruit.lua"
    
elseif ANYAxJPA == "BananaCatFarmBoss" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/OnlyBoss.lua"
    
elseif ANYAxJPA == "BananaCatKaitunFishit" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/Kaitun-FishIt.lua"
    
elseif ANYAxJPA == "BananaCatKaitunSAB" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/Kaitun-Sab.lua"
    
elseif ANYAxJPA == "BananaCatKaitun99N" then

    targetKey = getRealKey(bananaKeyBytes)
    targetURL = "https://raw.githubusercontent.com/obiiyeuem/vthangsitink/refs/heads/main/Kaitun-99night.lua"

elseif ANYAxJPA == "MaruAllMap" then

    targetKey = getRealKey(maruKeyBytes)
    targetURL = "https://raw.githubusercontent.com/xshiba/MaruBitkub/main/Mobile.lua"
    extraSetup = function() rawset(getgenv(), "id", "<id trong getgenv().id của maru") end
    
elseif ANYAxJPA == "MaruKaitun" then

    targetKey = getRealKey(maruKeyBytes)
    targetURL = "https://raw.githubusercontent.com/xshiba/MaruBitkub/main/Mobile.lua"
    extraSetup = function()
        rawset(getgenv(), "Script_Mode", "Kaitun_Script")
        rawset(getgenv(), "id", "<id trong getgenv().id của maru>")
    end
    
elseif ANYAxJPA == "MaruThaiLand" then

    targetKey = getRealKey(maruKeyBytes)
    targetURL = "https://raw.githubusercontent.com/xshiba/MaruBitkub/main/Mobile.lua"
    extraSetup = function()
        rawset(getgenv(), "id", "<id trong getgenv().id của maru>")
        rawset(_G, "Script_Language", "Thai")
    end
else
    
    targetURL = "https://raw.githubusercontent.com/Yeuemnhungkhongvoitoi/Missyou/refs/heads/main/ScriptError.txt"
end

if targetURL ~= "" then
    
    repeat task.wait() until game:IsLoaded() and game:GetService("Players").LocalPlayer

    if targetKey ~= "" then
        local oldPrint, oldWarn
        oldPrint = safe_hookfunc(print, function(...)
            local args = {...}
            for i, v in pairs(args) do
                if type(v) == "string" and string.find(v, targetKey) then args[i] = "[Protect By JPA]" end
            end
            return oldPrint(unpack(args))
        end)
        oldWarn = safe_hookfunc(warn, function(...)
            local args = {...}
            for i, v in pairs(args) do
                if type(v) == "string" and string.find(v, targetKey) then args[i] = "[Protect By JPA]" end
            end
            return oldWarn(unpack(args))
        end)

        rawset(getgenv(), "Key", targetKey)
    end
    
    extraSetup()

    safe_pcall(function()
        local scriptCode = safe_HttpGet(game, targetURL)
        local loadedFunction = safe_loadstring(scriptCode)
        scriptCode = nil 
        loadedFunction()
    end)

    if targetKey ~= "" then rawset(getgenv(), "Key", nil) end
    
    rawset(getgenv(), "id", nil)
    rawset(getgenv(), "Script_Mode", nil)
    rawset(_G, "Script_Language", nil)
    
    targetKey = nil
    bananaKeyBytes = nil
    maruKeyBytes = nil

    if clearconsole then
        clearconsole()
    elseif rconsoleclear then
        rconsoleclear()
    end
end
